name: scratch
root: ~/
tmux_options: -f ~/dotfiles/.tmux.conf
on_project_start: wal -R -e -n -q &

windows:
    - torrent: deluge-console
    - scratch:
    - media: ~/.scripts/mount.sh on
    - fetch: neofetch

