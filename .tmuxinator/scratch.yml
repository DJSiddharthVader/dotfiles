name: scratch
root: ~/
tmux_options: -f ~/dotfiles/.tmux.scratch.conf
on_project_start: wal -R -e -n -q &
on_project_exit: tmux kill-session -t scratch

windows:
    - tasks: tl
    - thesis: cd ~/thesis_SidReed
    - lab: conda activate thesis; unset BROWSER; jupyter lab
    - scratch: neofetch

