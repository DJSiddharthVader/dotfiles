# /home/sidreed/.config/tmuxinator/scratch.yml

name: scratch
root: ~/
tmux_options: -f ~/dotfiles/.tmux.conf
on_project_start: wal -R -e -n -q &

windows:
  - fetch: neofetch
  - media:
      panes:
        - watch -n1 'transmission-remote -tall -l | grep -v Idle | grep -v Stopped'
        - cd Torrents
  - tasks:
      root: ~/CMU_MSCB/Current/
        - taskwarrior-tui
  - scratch:
